БЭМ
https://ru.bem.info/
SCSS SASS
https://sass-lang.com/
BABEL
https://babeljs.io/
WEBPACK
https://webpack.js.org/
SMARTGRID
https://github.com/dmitry-lavrik/smart-grid
Yarn
https://classic.yarnpkg.com/en/docs/install#windows-stable
Chocolatey
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://chocolatey.org/install.ps1'))
CMD as administrator>
choco install chocolatey 
choco install yarn 
----------------------------------------------------------------------

https://tpverstak.ru/gulp/
node --version

Скорость и краткость конфигурации

gulp.task('styles', function() {
 return gulp.src('styles/*.scss')
 .pipe(sass({ style: 'expanded'  }))
 .pipe(rename({ suffix: 'min'  }))
 .pipe(minifycss() )
 .pipe(gulp.dest('build/styles/css'));
});

>Official web site
http://gulpjs.com
>catalog
http://gulpjs.com/plugins
>GIT
https://github.com/gulpjs.com/plugins/
>Документация Gulp (на английском) ENG
https://github.com/gulpjs/gulp/blob/master/docs/README.md#articles
Сборка Андрея Горохова

Процесс установки 
1) Устанавливаем NodeJS  и  Yarn 
2) Скачиваем сборку с помощью GIT:
Используем на WIN 10 готовую сборку Gulp 4 с помощью Chocolatey и Homebrew 
git clone https://github.com/andreyalexeich/gulp--scss-starter.git
3) Устанавливаем gulp глобально: yarn global add bem-tools-core
4) Переходим в скачанную папку со сборкой:
cd gulp-scss-starter
5) скачиваем необходимые зависимости: yarn
6) Чтобы начать работу, вводим команду: yarn run dev (режим разработки)
7) Чтобы собрать проект, вводим команду yarn run builder (режим сборки)

Если всё сделали правильно, должен открыться браузер с локальным сервером. Режим сборки предполагает оптимизацию проекта: сжатие изображений, минифицирование CSS и JS-файлов для загрузки на сервер. 

    Корень папки:
        .babelrc.js — настройки Babel
        .bemrc.js — настройки БЭМ
        .eslintrc.json — настройки ESLint
        .gitignore – запрет на отслеживание файлов Git’ом
        .stylelintrc — настройки Stylelint
        .stylelintignore – запрет на отслеживание файлов Stylelint’ом
        gulpfile.babel.js — настройки Gulp
        webpack.config.js — настройки Webpack
        package.json — список зависимостей
    Папка src — используется во время разработки:
        БЭМ-блоки: src/blocks
        шрифты: src/fonts
        изображения: src/img
        JS-файлы: src/js
        страницы сайта: src/views/pages
        SCSS-файлы: src/styles
        HTML-файлы: src/views
        конфигурационный файл веб-сервера Apache с настройками gzip (сжатие без потерь): src/.htaccess
    Папка dist — папка, из которой запускается локальный сервер для разработки (при запуске yarn run dev)
    Папка gulp-tasks — папка с Gulp-тасками

----------------------------------------------------------------------